<%= form_for(addv) do |f| %>
  <% if addv.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(addv.errors.count, "error") %> prohibited this addv from being saved:</h2>

      <ul>
      <% addv.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :image %>
    <%= f.file_field :image %>   
  </div>

  <div class="field">
    <%= f.label :organization_id %>
    <%= f.collection_select :organization_id, Organization.order(:name),:id,:name,include_blank: true%>
    
  </div>


  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
